<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/item_mail_linear"
    android:layout_width="wrap_content"
    android:layout_height="match_parent"
    android:gravity="center"
    android:orientation="vertical"
    android:padding="@dimen/item_detail_margin_top">

    <ImageView
        android:id="@+id/item_mail_replay_image"
        android:layout_width="@dimen/item_image_size"
        android:layout_height="@dimen/item_image_size"
        android:contentDescription="@string/app_name"
        android:scaleType="centerCrop"/>

    <TextView
        android:id="@+id/item_mail_replay_image_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:maxLength="@integer/max_length"
        android:textSize="@dimen/fong_normal"/>


    <TextView
        android:id="@+id/item_mail_replay_image_length"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:maxLength="@integer/max_length"
        android:textColor="@color/colorGray"
        android:textSize="@dimen/fong_small"/>

</LinearLayout>